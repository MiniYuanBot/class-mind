# 环境变量模板 —— 复制为 config/.env 并填入你的密钥（config/.env 已被 gitignore）。
# 与 paper-mind 共用同一套规范变量：同一份 config/.env 可直接在两仓库使用。
# 真实环境变量优先级高于本文件；classmind 的旧名（CLASSMIND_*）仍作为兼容别名。

# ---- 文本 LLM（DeepSeek / OpenAI 兼容）----
DEEPSEEK_API_KEY=sk-your-deepseek-key
# 默认即可，按需修改：
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_MAX_TOKENS=8192

# ---- 视觉图注（可选，Kimi / Moonshot 兼容端点）----
# 提示：先用下方命令确认你的账号可用模型，再把可用的视觉模型填入 KIMI_MODEL
#   curl -s https://api.moonshot.cn/v1/models -H "Authorization: Bearer $KIMI_API_KEY"
KIMI_API_KEY=sk-your-vision-key
# KIMI_BASE_URL=https://api.moonshot.cn/v1
# KIMI_MODEL=kimi-k3

# ---- 可选：用户技能（prompt 型 SKILL.md 文本注入）----
# 见仓库根 skills/README.md；可用分号分隔多个路径
# CLASSMIND_SKILLS=path/to/requirements.md;path/to/skills-dir
